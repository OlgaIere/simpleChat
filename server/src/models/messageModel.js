export default class MessageModel {
    constructor(name, text, className, time) {
        this.name = name;
        this.text = text;
        this.className = className;
        this.time = time;
    }
}